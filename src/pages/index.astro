---
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';
import { SITE_DESCRIPTION, SITE_TITLE } from '../consts';
import { getPublishedPosts } from '../lib/blog';

const posts = await getPublishedPosts();
const latestPosts = posts.slice(0, 6);
---

<!doctype html>
<html lang="ko">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
		<style>
			main {
				max-width: 720px;
			}
			.intro {
				margin-bottom: 3.5em;
			}
			.intro h1 {
				margin-bottom: 0.4em;
			}
			.intro p {
				color: rgb(var(--gray));
				font-size: 1.05em;
				line-height: 1.6;
				max-width: 600px;
			}
			.section-title {
				font-size: 1.5em;
				font-style: italic;
				margin-bottom: 1.8em;
				color: rgb(var(--gray-dark));
			}
			.posts-list {
				list-style: none;
				padding: 0;
				margin: 0 0 2.5em 0;
			}
			.post-item {
				margin-bottom: 2.5em;
			}
			.post-item a {
				text-decoration: none;
				color: inherit;
			}
			.post-item h3 {
				margin: 0 0 0.3em 0;
				color: rgb(var(--black));
				font-size: 1.35em;
			}
			.post-item h3:hover {
				color: var(--accent);
			}
			.post-date {
				color: rgb(var(--gray));
				font-size: 0.9em;
				margin-bottom: 0.5em;
			}
			.post-description {
				color: rgb(var(--gray));
				font-size: 1em;
				line-height: 1.5;
				margin: 0;
			}
			.view-all {
				margin-top: 1.5em;
			}
			@media (max-width: 720px) {
				main {
					max-width: 100%;
				}
				.intro {
					margin-bottom: 2em;
				}
				.section-title {
					margin-bottom: 1.2em;
				}
				.posts-list {
					margin: 0 0 1.5em 0;
				}
				.post-item {
					margin-bottom: 1.6em;
				}
			}
		</style>
	</head>
	<body>
		<Header />
		<main>
			<section class="intro">
				<p>매일 업데이트되는 글로벌 뉴스, 트렌드, 이슈를 큐레이션합니다.</p>
			</section>

			<section>
				<ul class="posts-list">
					{latestPosts.slice(0, 3).map((post) => (
						<li class="post-item">
							<a href={`/blog/${post.id}/`}>
								<h3 class="post-title title-clamp">{post.data.title}</h3>
							</a>
							<div class="post-date">
								{post.data.pubDate.toLocaleDateString('ko-KR', { year: 'numeric', month: 'long', day: 'numeric' })}
							</div>
							<p class="post-description">{post.data.description}</p>
						</li>
					))}
				</ul>
				<div class="view-all">
					<a class="button primary" href="/blog">Show all</a>
				</div>
			</section>
		</main>
		<Footer />
	</body>
</html>
